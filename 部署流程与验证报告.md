# 多标签页 + 视觉美化 · 部署流程与验证报告

> 项目：征收调查系统（zscx）
> 测试服务器：`REDACTED_IP`
> 变更内容：右侧主页面多标签页系统（keep-alive）+ 整体视觉美化
> 部署日期：2026-08-13

---

## 一、本次变更内容

### 1. 多标签页系统（keep-alive，保留各页状态）

| 文件 | 说明 |
| --- | --- |
| `web/components/layout/tabs-context.tsx` | 标签状态管理：`TabsProvider` 维护已打开标签、活动标签、关闭/聚焦逻辑；路由解析为标签 key（数据表页含 `module` 区分调查/征收，审批页含 `tab` 区分视图）。首页「仪表盘」为常驻不可关闭标签。 |
| `web/components/layout/tabs-bar.tsx` | 顶部标签栏：点击切换、关闭（X）、超长横向滚动。 |
| `web/components/layout/tabs-panes.tsx` | 主内容区：所有已打开标签的页面**始终挂载**，非活动页用 `display:none` 隐藏，切换时各页数据/筛选/滚动状态完整保留。 |
| `web/components/layout/workspace-shell.tsx` | 用 `TabsProvider` 包裹，右侧由 `Header` + `TabPanes` 组成。 |
| `web/components/layout/sidebar.tsx` | 数据表链接点击时预注册标签标题（如「住户（调查）」）；静态页面标题自动从内置路由表解析。 |

### 2. 视觉美化（现代简洁专业风）

| 文件 | 说明 |
| --- | --- |
| `web/components/layout/header.tsx` | 半透明阴影、在线状态点、日期显示。 |
| `web/components/layout/sidebar.tsx` | 品牌区渐变徽标、用户区圆角头像、子项左缩进。 |
| `web/app/globals.css` | 全局精致滚动条、文字抗锯齿。 |

> 说明：`/dashboard` 与 `/approval` 均使用 `WorkspaceShell`，标签系统对两处一致生效。

---

## 二、部署流程

### 0. 前置条件
- 本地需安装 `paramiko`（`pip install paramiko`）。
- 测试服务器 SSH：`REDACTED_USER@REDACTED_IP:22`。

### 1. 上传变更文件到服务器源码目录
本地文件 → 服务器 `/vol2/1000/docker/zscx/web/`，本次共 7 个文件：

```
app/globals.css
components/layout/sidebar.tsx
components/layout/header.tsx
components/layout/workspace-shell.tsx
components/layout/tabs-context.tsx
components/layout/tabs-bar.tsx
components/layout/tabs-panes.tsx
```

> 上传后逐文件校验 `local size == remote size`，全部一致。

### 2. 构建镜像
在含 build 上下文的 compose 目录构建 `zscx-web:local` 镜像：

```bash
cd /vol2/1000/docker/zscx/docker
docker compose build web
```

> build context 指向 `../web`，即上传后的源码，构建结果命名 `docker.io/library/zscx-web:local`。

### 3. 用新镜像重启 web 容器
> ⚠️ 关键：运行中的容器由 `docker-nas` 项目管理，而非 `docker` 目录下的 compose。

通过 `docker inspect` 标签确认：

```
project=docker-nas
workdir=/vol2/1000/docker/zscx/docker-nas
files=/vol2/1000/docker/zscx/docker-nas/docker-compose.yml
service=web
```

重启（`--no-deps` 避免触碰 mysql/redis，`--force-recreate` 用新镜像重建）：

```bash
cd /vol2/1000/docker/zscx/docker-nas
docker compose up -d --no-deps --force-recreate web
```

### 4. 检查容器状态
```bash
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}' | grep zscx
```

---

## 三、验证报告

### 1. 容器状态
```
zscx-web     zscx-web:local   Up 13 seconds
zscx-redis   redis:7-alpine   Up 2 hours (healthy)
zscx-mysql   mysql:5.7        Up 2 hours (healthy)
```

### 2. HTTP 连通性
```
--- login ---
200
--- dashboard (follow redirects) ---
200
```

### 3. 应用启动日志（`docker logs zscx-web`）
```
> zscx-web@1.2.2 start
> next start

  ▲ Next.js 14.2.5
  - Local:        http://localhost:3000

 ✓ Starting...
 ✓ Ready in 811ms
[Prisma] Creating client with URL: mysql://zscx:zscx123456@mysql:3306/zscx...
[Redis] Connected successfully
```
- Next.js 启动成功；
- 数据库迁移完成（`✅ 数据库迁移完成！`）；
- Redis 连接成功；
- 日志无报错。

### 4. 访问地址
**http://REDACTED_IP:777**（需登录后验证）

### 5. 功能/视觉验证清单（人工确认项）
- [ ] 依次点击多个菜单，顶部出现多个标签；
- [ ] 点击标签可瞬间切换，各页数据/筛选/滚动状态保留（keep-alive）；
- [ ] 关闭按钮（X）可关标签，关闭当前页自动跳转相邻标签；
- [ ] 「仪表盘」为常驻不可关闭标签；
- [ ] 侧边栏渐变徽标、折叠分组（主栏目点击展开/收起二级子项）；
- [ ] 顶部标签栏、头部在线状态点 + 日期、全局精致滚动条。

---

## 四、复用说明

部署脚本：[`deploy_tabs.py`](./deploy_tabs.py)

脚本已按正确的两步流程修正：
1. 上传文件；
2. `docker compose build web`（在 `docker` 目录构建镜像）；
3. `docker compose up -d --no-deps --force-recreate web`（在 `docker-nas` 目录重启）；
4. 检查容器状态。

> 复用方式：更新脚本顶部的 `FILES` 列表后执行 `python deploy_tabs.py`。

---

## 五、注意事项与已知取舍

- **keep-alive 的资源占用**：已打开标签的页面会保持挂载，其自动刷新/轮询会继续运行；若担心多开资源占用，可改为「简单切换（切换时重新加载）」模式。
- **标签切换与地址栏**：点击标签切换为纯前端切换，不改变地址栏 URL；关闭当前活动标签会自动跳转到相邻标签。
- **刷新行为**：浏览器刷新后活动标签按当前 URL 重新聚焦。
